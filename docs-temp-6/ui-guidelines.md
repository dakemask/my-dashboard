# UI 准则

- 图标统一使用字节跳动 IconPark 的 `@icon-park/svg`，由 TypeScript 引用并通过 Vite 直接编译进构建产物。纯图标按钮必须同时提供准确的 `aria-label` 和 `title`。

- 所有 UI 的内联文字编辑必须“无感”：输入文字与非焦点显示等场景下，文字的字号、行高，以及编辑区域的宽高、内外边距等都应与只读态保持基本一致，不得发生布局跳动。实现时优先让同一输入控件在只读态与编辑态间切换。
