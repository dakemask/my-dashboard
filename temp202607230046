User:

我在考虑AGENTS.md怎么写。AGENTS.md（以及其引用的其他文档）才是长久的，现在这四份文档是暂时的。另外除了mindmap和fragmentthoughts之外，后续新模块开发应该不需要指导文档了，现在四份文档里说需要从零开发指导文档，AGENTS.md中应该删掉这条。

我想AGENTS.md应当是一个入口，其应该主要引用其他文档，其他文档存放在agents/下。

我大概写一下相关文档的结构，你判断行还是不行。

AGENTS.md的结构：

（对项目的介绍，其是一个个人仪表盘，包含许多模块）

（目前包括fragment-thoughts（记录碎片想法）、mind-maps（绘制思维导图）、...）

（项目的源代码会部署到Github的公开仓库my-dashboard上，通过Github action实现上传后自动构建并部署到Github pages。就可以通过外部访问）

（项目中有一些模块会涉及数据的保存，数据存放在另一个私人仓库my-dashboard-data中，项目通过token访问，以实现用户的隐私与跨设备访问模块数据）

（对于那些涉及数据保存的模块，必须使用项目提供的SDK，并遵守配套的开发约束，详情见xx、xx）（不需要数据保存的模块则不用使用这个SDK）

（agent不应当在用户未允许的情况下改动shared，如果需要改动，请阅读xx）

（A模块的相关开发见xx）

（B模块的相关开发见xx）

然后是SDK说明与配套开发约束的结构：

（对五层模型和数据流动方式的介绍）（不涉及SDK代码细节）

（对撤销队列的介绍）（只涉及如何使用SDK暴露的接口）

（对初始化的介绍）（只涉及如何使用SDK暴露的接口）

（对本地保存与云端同步的介绍）（只涉及如何使用SDK暴露的接口）

（其他细节）

新模块开发时做的事应该放在另一个文档中，这个文档里才是涉及更多SDK代码细节的，并且初版模块开发完后就不用看这个了：

（第一步定义什么）

（第二步定义什么）

（第三步定义什么）

.......

然后是维护shared内部实现。

shared内部实现文档应该和其他项目具体模块的文档差不多。

项目具体模块如果使用SDK，文档中应该包含相关的定义。

Assistant:

整体结构可行，而且比现在四份文档的定位更清楚：`AGENTS.md` 只负责介绍项目、规定最高层规则、把不同任务分流到对应文档；具体规则放在 `agents/`。

需要调整的关键点有三个：

1. 不再要求每个模块都有“从零开发文档”

这条应该从长期规则中删除。

只有以下情况才需要模块文档：

- Mind Map 和 Fragment Thoughts 当前确实需要从零重写说明；
- 模块存在长期有效的特殊业务规则；
- 模块使用持久化 SDK，并且需要记录自己的 payload、event、容量、结算方式等定义；
- 模块本身足够复杂，需要维护说明。

普通、简单、无持久化的新模块不必为了形式单独创建文档。

2. 区分“长期公共契约”和“新模块接入教程”

这两个文档的读者和寿命不同：

- 公共契约：开发或维护任何持久化模块时都需要读，长期有效；
- 新模块接入教程：只有第一次创建持久化模块时读，模块完成后不必再读。

因此 AGENTS 中不应该把“新模块接入教程”列为所有模块开发的必读文档，只在“创建新的持久化模块”任务下引用。

3. 防止同一规则存在两份真相

迁移完成后，当前 `docs/` 中的四份临时文档应该删除，或者明确标记废弃。不要让它们与 `agents/` 中的新文档同时存在，否则后续 agent 不知道听哪一份。

建议目录结构如下：

```text
AGENTS.md

agents/
  persistent-module-contract.md
  create-persistent-module.md
  shared-internals.md

  modules/
    mind-maps.md
    fragment-thoughts.md
```

其中：

- `persistent-module-contract.md`：长期公共约束，只讲五层模型和 SDK 公共接口。
- `create-persistent-module.md`：第一次创建持久化模块的操作教程。
- `shared-internals.md`：只有获准修改 Shared 时才读。
- `modules/*.md`：模块长期有效的特有定义与规则。

## AGENTS.md 建议结构

你写的内容基本正确，可以整理为：

```text
1. 项目是什么
   - 个人仪表盘
   - 由多个相互独立的模块组成
   - 当前有哪些模块及其用途

2. 部署和数据仓库
   - my-dashboard 是公开源码仓库
   - GitHub Actions 自动构建并部署到 GitHub Pages
   - my-dashboard-data 是私人数据仓库
   - 登录用户通过 token 访问私人仓库

3. 模块分类
   - 需要保存数据的模块必须使用 Shared SDK
   - 不需要保存数据的模块不要求使用该 SDK

4. Shared 修改权限
   - 普通模块开发不得顺便修改 Shared
   - 只有用户明确要求，或任务无法在公共接口内完成并获得用户同意后，才能修改
   - 修改前必须阅读 agents/shared-internals.md

5. 按任务分流
   - 开发或维护持久化模块：
     读 agents/persistent-module-contract.md
   - 第一次创建新的持久化模块：
     额外读 agents/create-persistent-module.md
   - 开发或维护 Mind Map：
     额外读 agents/modules/mind-maps.md
   - 开发或维护 Fragment Thoughts：
     额外读 agents/modules/fragment-thoughts.md
   - 修改 Shared：
     读 agents/shared-internals.md

6. 通用安全规则
   - 未经允许不访问或修改真实私人仓库数据
   - token 不得进入日志、DOM、错误文本和业务数据
   - 测试必须使用模拟 GitHub 请求
```

这样 `AGENTS.md` 会很短，也不会塞入容易过时的实现细节。

## 持久化模块公共契约

你提出的结构是正确的。建议包含：

- 五层模型及数据流动；
- payload、实时状态和 event 的区别；
- event 历史、容量、撤销和重做；
- `dispatch`、`settle` 和 `project` 的语义；
- 初始化；
- 本地保存；
- 上传、拉取和冲突；
- spinner、阻塞和编辑锁由 SDK 负责；
- 生命周期和 `dispose`；
- 模块可以使用的公共接口；
- 禁止直接访问 IndexedDB、GitHub、token 或 Shared 内部组件。

这里不应该出现 Git Data API、IndexedDB 表结构、CAS 算法或轮询器实现。

## 新持久化模块接入教程

这个文档才按步骤展开：

1. 定义完整 payload；
2. 定义业务 event；
3. 选择历史容量；
4. 实现 `apply` 和 `invert`；
5. 定义 `validate`、`contentKey`、`encode`、`decode`；
6. 实现 `settle` 和 `project`；
7. 调用 `startModuleRuntime`；
8. 连接编辑动作与 `runtime.dispatch`；
9. 连接保存、同步和冲突 UI；
10. 根据模块需要自行绑定快捷键；
11. 注册模块入口；
12. 编写模块验收测试。

初版开发完成后，维护该模块不需要再读这份教程。

## 模块文档

使用 SDK 的模块文档建议记录：

- `moduleId`；
- 完整 payload 的业务边界；
- event 类型及其业务含义；
- `history.capacity` 的选择和理由；
- `apply`/`invert` 规则；
- 远端受管文件；
- 六种 `settle` 情况下的模块特有行为；
- `project` 如何重置实时状态；
- 模块自己采用的快捷键；
- 冲突 UI；
- 模块特有验收标准。

不建议在文档里长期复制大段 TypeScript 实现。文档记录语义定义，源代码类型是可执行的最终真相；如果确实复制了代码定义，两者必须在同一次修改中更新。

因此，你提出的总体方向是合适的。最重要的调整就是：把“每个模块都要有从零开发文档”改成按任务按需阅读，并明确公共契约、初次接入教程、Shared 内部说明和模块专用说明之间的边界。

User:

你能再给我回复一次吗，我感觉你上次回复有些在重复我已经提出的东西，比如所谓需要调整的关键点，你没必要再煞有介事地告诉我了。
然后你列举的持久化模块公共契约太散了。我之前提出的那几条已经是我考虑过的了，我希望你要么肯定我列举的那几个就完全够了，要么提出一点更具有建设性的意见。
另外你列举的模块文档里的哪些条目感觉也“不太认真”（虽然我知道你作为一个llm没有认不认真的概念，只是我之前没跟你说要注意这个），如果按你这样写，想必会有些和之前持久化模块契约重复；然后也没有关于模块本身逻辑的内容。最重要的是没有提出标准格式，给我一种不太认真的感觉。

Assistant:

可以。更准确地说，你设计的分层是成立的：

- `AGENTS.md` 是项目入口和任务路由。
- SDK 公共契约是持久化模块长期遵守的规则。
- 新模块接入文档是只在初次开发时使用的操作手册。
- Shared 内部文档只服务于 Shared 维护。
- 模块文档描述模块本身，而不是复述通用规则。

## SDK 公共契约

你列出的内容作为主体已经够了。与其继续增加零散章节，我只建议把它们组织成一条完整生命周期：

1. 五层模型与职责边界；
2. 初始化；
3. 编辑、event 历史、撤销和重做；
4. 本地保存；
5. 云端同步与冲突；
6. 页面结束和失败时的保证。

其中每个阶段使用相同格式：

```text
触发条件
模块需要做什么
SDK 会做什么
完成后哪些状态发生变化
失败时哪些状态必须保持不变
```

例如“本地保存”不需要再分散介绍 spinner、IndexedDB、settle、dirty，而是集中写成：

```text
触发：模块调用 runtime.save()

模块：
- 在 settle("local-save") 中结束实时交互并按需返回 event

SDK：
- dispatch settle 返回的 event
- 阻止页面交互
- 将当前完整 payload 保存到本地
- 不清空 event 历史

成功结果：
- 当前 payload 成为本地保存基线
- dirty 为 false

失败结果：
- payload、event 历史和原保存基线不变
- 页面恢复交互
```

这样文档既完整，也不会变成公共 API、内部实现和 UI 细节的杂烩。

文档最后只需要一张公共接口索引：

```text
dispatch
undo
redo
save
upload
pull
resolveConflict
dispose
```

这里解释接口的语义，不放具体接入代码。

## 新持久化模块接入文档

这个文档可以保留在 `agents/`，因为它对未来每次创建模块都能复用；只是现有模块维护时不需要读。

建议每一步都采用固定格式：

```text
本步目标
需要创建或修改的文件
需要实现的公共接口
最小代码示例
必须编写的测试
本步完成标准
```

正文按以下顺序即可：

1. 定义 payload；
2. 定义 event；
3. 定义 `apply`、`invert` 和历史容量；
4. 定义校验、content key 和远端编码；
5. 实现 `settle` 和 `project`；
6. 启动 runtime；
7. 将业务动作连接到 `dispatch`；
8. 接入保存、同步和冲突 UI；
9. 根据模块需求绑定按钮和快捷键；
10. 注册模块并完成测试。

它不再解释五层模型为什么如此设计，只引用公共契约。

## 模块文档的标准格式

模块文档首先应描述模块，而不是从 SDK 开始。建议所有需要长期文档的模块使用下面的标准格式：

```markdown
# <模块名称>

## 1. 模块目标

### 用户要解决的问题

### 核心使用场景

### 明确不做的事情

## 2. 业务模型

### 核心实体

### 实体之间的关系

### 身份、顺序和归属规则

### 必须始终成立的业务不变量

## 3. 用户界面与操作流程

### 页面和主要区域

### 创建、编辑、删除等核心流程

### 选择、焦点、拖拽、文本编辑等交互状态

### 操作何时算完成

### 模块自己的快捷键

## 4. 模块内部架构

### 主要组件及职责

### 状态由谁持有

### 组件之间的数据流

### 目录和关键源码入口

### 允许依赖与禁止依赖

## 5. 持久化 SDK 定义

> 不使用 SDK 的模块删除本章。

### moduleId

### Payload 定义

放实际的类型定义，并解释各字段的业务含义。

### Event 定义

| Event | 用户动作 | 提交时机 | 对 payload 的影响 | 撤销所需信息 |
| ----- | -------- | -------- | ----------------- | ------------ |

### 历史容量

记录具体选择和理由，不解释通用的容量规则。

### settle 行为

| reason | 模块需要结束或取消什么 | 返回的 event |
| ------ | ---------------------- | ------------ |

### project 行为

记录模块在初始化、撤销和重做后如何重建界面、重置哪些实时状态。

### 远端编码

记录受管文件、每个文件包含什么，以及 payload 如何映射到这些文件。

### 冲突界面

只记录该模块如何向用户呈现两个覆盖方向。

## 6. 异常与边界情况

### 损坏数据

### 不完整操作

### 空状态

### 大数据量

### 模块特有失败行为

## 7. 验收标准

### 业务逻辑

### 交互

### Payload/Event

### SDK 接入

### 模块特有测试
```

这套格式解决了两个问题：

- 前四章描述模块本身的业务逻辑、交互和内部结构；
- 第五章只记录该模块对 SDK 公共契约作出的具体选择。

模块文档不应再次解释什么是五层模型、为什么 event 不进 IndexedDB、SDK 如何实现冲突等通用内容。它只写：

```text
本模块的 payload 是什么
本模块有哪些 event
本模块选择多少历史容量
本模块在 settle 时具体做什么
```

如果文档包含准确的 Payload/Event 类型，那么应规定：相关源码类型发生变化时，必须在同一次任务中更新模块文档。

## Shared 内部文档

Shared 文档可以和模块文档采用相同的上层逻辑：

```text
目标
领域模型
主要流程
内部架构
异常与不变量
验收标准
```

但它需要额外增加两个 Shared 特有章节：

```text
公共接口兼容性
内部组件与数据格式
```

因此建议格式为：

1. Shared 的职责与非职责；
2. 对业务模块提供的公共接口；
3. 内部组件及依赖方向；
4. 历史、IndexedDB、GitHub、同步和锁的内部状态模型；
5. 关键算法与原子性；
6. 安全及失败不变量；
7. 修改公共接口时的兼容性要求；
8. 测试矩阵。

## AGENTS.md 最终形态

`AGENTS.md` 本身只需要三个结构化部分：

```text
项目及仓库简介

模块目录表：
moduleId | 用途 | 是否持久化 | 模块文档

任务路由表：
任务类型 | 必须阅读的 agents 文档

少量最高级硬规则：
Shared 修改权限、真实数据安全、token 安全
```

“每个模块都必须有从零开发指导文档”不应出现。

更准确的规则是：

- 创建新的持久化模块时，阅读通用接入文档；
- 维护已有模块时，不读接入文档；
- 模块存在长期专用文档时，阅读该模块文档；
- 简单模块不强制创建模块文档；
- 使用 SDK 且具有模块文档时，文档必须采用标准的“持久化 SDK 定义”章节。
